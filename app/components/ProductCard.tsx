import React from 'react';
import AddToCart from './AddToCart';


const ProductCard = () => {
  return (
    <div> 
      <AddToCart />
    </div>
  );//after 'p-', enter Ctrl + Space to get the values
}

export default ProductCard;
